import express from 'express';

const router = express.Router();

router.get("/",)
router.get("/:stayId",)
// router.put("/:stayId",)//ownerOnly
// router.post("/",)//hostOnly
// router.delete("/:stayId",)

export const stayRoutes = router;